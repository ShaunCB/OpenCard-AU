# Prevent workerd (wrangler's local dev runtime) from running its platform-specific
# install script on machines where the binary isn't available (e.g. Windows ARM64).
# This only affects `wrangler dev` local mode — `wrangler deploy` works on all platforms.
ignore-scripts=false
